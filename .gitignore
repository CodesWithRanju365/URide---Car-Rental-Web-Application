# Node.js dependencies
node_modules/

# Build output from Next.js
.next/

# Production build
build/

# Vercel deployment folder (if you're using Vercel for deployment)
.vercel/

# Testing output
coverage/

# System files (macOS, Linux, Windows)
.DS_Store
Thumbs.db

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Sensitive environment variables files (can be optionally committed if needed)
.env*
!.env.example   # If you want to keep a sample .env file

# TypeScript build info (optional, if you're using TypeScript)
*.tsbuildinfo
next-env.d.ts

# Yarn and Pnpm lock files (optional, if using Yarn or Pnpm)
yarn.lock
pnpm-lock.yaml

# IDE/Editor folders and files (VSCode, JetBrains, etc.)
.idea/
.vscode/

# Optional: Ignore other potential lock files (optional)
.pnp
.pnp.*

# Miscellaneous folder/file ignores
*.pem            # Ignore private key files (if present)
*.log            # Ignore any log files not included in npm-debug.log*
*.tgz            # Ignore tarball files generated by npm or yarn
*.bak            # Ignore backup files
*.swp            # Ignore swap files

# Optional: Ignore other possible Node.js related files
npm-shrinkwrap.json
package-lock.json

# Other files (optional based on project)
*.env.*          # Ignore environment files that could contain sensitive data
